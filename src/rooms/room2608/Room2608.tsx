/**
 * Sala 2608. Todavia vacia.
 *
 * Se llega desde el room99 atravesando el blanco, asi que ARRANCA en blanco:
 * el salto entre salas no debe verse. Cuando haya algo que poner, entrara
 * desde aqui.
 */
export default function Room2608() {
  return (
    <div
      style={{
        position: "fixed",
        inset: 0,
        background: "#ffffff",
      }}
    />
  );
}
